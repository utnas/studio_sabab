StudioSabab.IndexRoute = Ember.Route.extend({
    /*redirection: function(){ 
    	var noSubState = !(this.router.location.getURL().split('/')[1]);
	    if (noSubState) {
	        this.transitionTo('index');
	    }
  	}*/
});