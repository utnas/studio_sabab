StudioSabab.IndexRoute = Ember.Route.extend({
    /*debugger;
    var noSubState = !(router.location.getURL().split('/')[1]);
    if (noSubState) {
        router.transitionTo('index');
    }
    renderTemplate: function(){
  		this.render('index');
  	}*/
});